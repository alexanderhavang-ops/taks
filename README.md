# TAKS Overview

TAKS is a framework for deploying and operating TAK nodes with an installer-owned runtime.

## Components
- **orchestrator/**: Control plane (UI/backend)
- **tak-installer/**: Node runtime installer (nginx, systemd, takctl sync)
- **takctl/**: CLI + Web backend

