Return EXACTLY one JSON object and nothing else:

{"sql":"SELECT ..."}

Rules:
- Only key allowed: sql
- No prose, no examples, no markdown
- Must be valid JSON
- SQL MUST include a column alias using AS <name>

SQL must answer the question.
Do NOT return placeholder SQL like: SELECT 1, SELECT true, SELECT now()

SQL safety:
- Read-only only (SELECT or WITH)
- Single statement
- No semicolons
- No writes / DDL / COPY

Question (JSON input):
{{CONTEXT_JSON}}

