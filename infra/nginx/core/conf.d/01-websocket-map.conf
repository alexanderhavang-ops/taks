# Installer-owned helper for WebSocket upgrade handling.
# Used by the :8446 frontdoor catch-all proxy (TakServer /takproto websocket).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
