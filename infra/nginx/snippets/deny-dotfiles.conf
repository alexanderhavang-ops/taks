# Block VCS + hidden files everywhere (installer-owned)
location ~* (^|/)\.(?!well-known/).* { deny all; return 404; }
location ^~ /.git/ { deny all; return 404; }
location = /.git { deny all; return 404; }
