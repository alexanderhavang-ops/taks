[Service]
# Load DB connection settings for takctl-web (installer-owned).
# The '-' makes it optional so apply won't brick if file is missing early.
EnvironmentFile=-/opt/tak/tools/takctl/secrets/db.env
