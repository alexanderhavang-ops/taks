function CertsView() {
  return h("div", { className: "card" },
    h("div", { className: "card-title" }, "Certificates"),
    h("div", { className: "muted" }, "Coming soon (next step: /api/certs + table)")
  );
}
