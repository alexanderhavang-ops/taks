Render a detailed HTML timeline view for domain "timeline" from the provided structured timeline JSON.

Use:
- headline
- window
- lanes
- render_hints

Rendering guidance:
- large SVG with clean swimlanes
- concise title and subtitle above the SVG
- show only short visible labels
- use <title> for summaries
- stack markers only when items share nearly the same timestamp in the same lane
- do not narrate geometry
- do not include comments
- do not include explanatory text outside the chart except title/subtitle

If input items are outside the 48-hour window, do not render them.
If a lane has no visible items in the 48-hour window, omit visible lane content for it.
