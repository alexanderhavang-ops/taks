# Minimal packages for a TAK node (Ubuntu 24.04)
# This list is intentionally small; we’ll grow it only when we prove we need more.

# Base tooling
ca-certificates
curl
jq
openssl
unzip

# Java runtime for TAK
openjdk-17-jre-headless

# PostgreSQL local on node (pin major version for TAK)
postgresql-15
postgresql-client-15
postgresql-15-postgis-3

# NGINX edge
nginx

# Config manipulation/validation (optional but useful)
xmlstarlet

# Sendmail-compatible lightweight SMTP client for onboarding email delivery
msmtp-mta

# NOTE: Let's Encrypt is NOT offline-friendly. In offline mode, you’ll use internal CA instead.
# certbot
# python3-certbot-nginx
